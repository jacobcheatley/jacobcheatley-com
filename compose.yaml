services:
  db:
    image: postgres:18
    environment: { POSTGRES_PASSWORD: postgres, POSTGRES_DB: app }
    ports: ["5432:5432"]
    volumes: [db:/var/lib/postgresql] # 18+ volume path, not /var/lib/postgresql/data
volumes: { db: {} }
