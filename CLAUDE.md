# jacobcheatley.com

Personal website for fun projects and learning. See `README.md` for the tech stack.

## Agent skills

### Issue tracker

Issues are tracked as GitHub Issues on `jacobcheatley/jacobcheatley-com` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. Neither exists yet; revisit after the initial architecture is decided. See `docs/agents/domain.md`.
