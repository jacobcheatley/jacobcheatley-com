// The lines more than one screen of the Project writes the same way.

// A share of the crowd, which is always read as a whole percentage.
export const percent = (share: number) => `${Math.round(share * 100)}%`;
