#!/bin/sh
if ! command -v gitleaks >/dev/null 2>&1; then
  echo "pre-commit: gitleaks not found on PATH — install it (see README) so secrets can be scanned." >&2
  echo "To bypass in an emergency: git commit --no-verify" >&2
  exit 1
fi
exec gitleaks git --staged --no-banner --redact .
